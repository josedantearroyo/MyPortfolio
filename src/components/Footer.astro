---
import { Icon } from "astro-icon/components"
const currentYear = new Date().getFullYear()
---

<footer class="w-full mt-8">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Contenido centrado del footer -->
    <div class="py-6 sm:py-8">
      <div class="flex flex-col items-center justify-center text-center space-y-3 sm:space-y-4">
        

        <!-- Copyright y información -->
        <div class="text-center space-y-2">
          <p class="text-sm sm:text-base text-gray-500 dark:text-gray-400">
            &copy; {currentYear} José Dante Arroyo.
          </p>
          <p class="text-xs sm:text-sm text-gray-400 dark:text-gray-500">
            Desarrollado con ❤️ usando Astro y Tailwind CSS
          </p>
        </div>

        <!-- Botón subir -->
        <button 
          onclick="window.scrollTo({top: 0, behavior: 'smooth'})"
          class="text-sm text-gray-500 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors duration-200 flex items-center gap-2 hover:scale-105 transform"
        >
          <Icon name="tabler:arrow-up" class="w-4 h-4" />
          Volver arriba
        </button>
      </div>
    </div>
  </div>
</footer>

<style>
  /* Efectos hover para iconos sociales */
  footer a[aria-label]:hover {
    transform: translateY(-2px) scale(1.1);
  }

  /* Efectos hover para botón subir */
  footer button:hover {
    transform: translateY(-1px) scale(1.05);
  }

  /* Responsive adjustments */
  @media (max-width: 640px) {
    .space-y-4 {
      gap: 1rem;
    }
    
    .gap-4 {
      gap: 1rem;
    }
  }

  /* Mejorar legibilidad en pantallas muy pequeñas */
  @media (max-width: 375px) {
    .px-4 {
      padding-left: 1rem;
      padding-right: 1rem;
    }
    
    .py-8 {
      padding-top: 1.5rem;
      padding-bottom: 1.5rem;
    }
  }
</style>